apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    testCompile (
        "junit:junit:4.12",
        "org.hamcrest:hamcrest-all:1.3",
        "org.junit.contrib:junit-theories:4.12",
        "com.pholser:junit-quickcheck-core:0.6",
        "com.pholser:junit-quickcheck-generators:0.6"
    )
}
